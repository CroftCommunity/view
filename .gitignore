# Local, per-repo Claude Code permission allowlist — not shared.
.claude/settings.local.json

# macOS
.DS_Store

# Node / build
node_modules/
dist/

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/

# Scene originals never enter git — they live on R2 (video) and are dropped
# locally by the owner (build plan 4.3b / 6). mediaBase is the only deploy switch.
media/
posters/
