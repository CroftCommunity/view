# Changelog — View

What changed for someone with a View on a screen. It deploys from `main` (Pages), so
landing *is* releasing: sections are months, each entry dated by its landing. Per
`CroftC/.claude/CHANGELOGS.md`, the branch that changes something a viewer runs adds its
entry here before it lands. Started 2026-08-29; earlier history is in `git log`.

## 2026-08

- 2026-08-26 Tap targets meet the 44px floor (was 40), and the accessibility scan is
  hermetic — the same DOM on every runner.
- 2026-08-25 Playwright on the workspace standard (`^1.61.1`, resolved 1.61.1 pinned).
