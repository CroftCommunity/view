# view — agent orientation

## Identity (workspace architecture)

**Scope:** ambient scenery views for wall displays/kiosks — static, ad-free, per-scene licensed.
**Not this repo:** interactive apps (the satellite products own those).
Card + altitudes: `CroftC/.claude/ARCHITECTURE.md`. Concurrent-session norms:
`CroftC/.claude/COORDINATION.md` (multi-turn work in `worktrees/view/<slug>`,
never this checkout).
